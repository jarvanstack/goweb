package utest

import (
	"fmt"
	"testing"
)

//通过二进制的 & 比较
//不能直接比较
//strings.compare() 更快,我们就用这个来比较.
func Test_binary_compare_test(t *testing.T) {
	// s1 := "abc"
	// b2 := []byte("abc")
	// b1 := []byte(s1)
	// r := b1[:] && b2[:]
	// fmt.Printf("r=%#v\n", r)
	fmt.Printf("%s\n", "")
}
